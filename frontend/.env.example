# Frontend — local development (npm run dev)
# Copy to .env:  cp .env.example .env
# When using Docker, env comes from project root .env — no need to copy here.

# Leave empty when using Vite proxy (default: proxies to localhost:5000).
# Set when backend runs elsewhere:
# VITE_API_URL=http://localhost:5000
# VITE_SOCKET_URL=http://localhost:5000

VITE_API_URL=
VITE_SOCKET_URL=
