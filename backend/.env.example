# Copy to .env and fill in your values when running the backend locally (e.g. python app.py).
# When using Docker: env is set from the project root .env by Compose; you do not need to copy .env here.

# Deepgram API key (required for live transcription).
# Get one at https://console.deepgram.com
DEEPGRAM_API_KEY=your_deepgram_api_key_here

# Flask secret key (used for sessions; also fallback for JWT if JWT_SECRET unset)
SECRET_KEY=dev-secret-change-in-production

# JWT signing secret for auth tokens (optional; falls back to SECRET_KEY if unset)
# JWT_SECRET=your-jwt-secret-here

# MongoDB connection string.
# Local: mongodb://localhost:27017
# Atlas:  mongodb+srv://USER:PASSWORD@cluster.xxxxx.mongodb.net/?retryWrites=true&w=majority
MONGODB_URI=mongodb://localhost:27017

# MongoDB database name (optional; default: meeting_transcription)
# MONGODB_DB_NAME=meeting_transcription

# You.com API key (optional; for AI meeting summarization via Express Agent).
# Get one at https://you.com/platform
# YOUCOM_API_KEY=your_youcom_api_key_here

# Foxit APIs (optional; for PDF meeting report generation).
# Document Generation and PDF Services use separate credentials. Get at https://developers.foxit.com
# FOXIT_CLIENT_ID=your_docgen_client_id
# FOXIT_CLIENT_SECRET=your_docgen_client_secret
# FOXIT_PDF_SERVICES_CLIENT_ID=your_pdf_services_client_id
# FOXIT_PDF_SERVICES_CLIENT_SECRET=your_pdf_services_client_secret
# FOXIT_HOST=https://na1.fusion.foxit.com
