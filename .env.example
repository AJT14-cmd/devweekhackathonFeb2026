# For Docker Compose: copy to .env to override only what you need.

# Deepgram API key (for live mic transcription)
# Example: DEEPGRAM_API_KEY=abc123def456789...
DEEPGRAM_API_KEY=your_deepgram_api_key_here

# DeepSeek API key (for summarization: key insights, decisions, action items). Optional; without it, summary is first 500 chars of transcript.
# Example: DEEPSEEK_API_KEY=sk-...
# DEEPSEEK_API_KEY=
# Optional: DEEPSEEK_BASE_URL=https://api.deepseek.com (default)
# Optional: DEEPSEEK_MODEL=deepseek-chat (default)

# MongoDB (optional): default is local mongo in docker-compose. Uncomment to use Atlas.
# Example local:  MONGODB_URI=mongodb://mongo:27017
# Example Atlas:  MONGODB_URI=mongodb+srv://user:pass@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority
# MONGODB_URI=
# MONGODB_DB_NAME is empty by default; set only if you want a specific database name, e.g.:
# MONGODB_DB_NAME=meeting_transcription

# Optional: override backend secrets (defaults are in docker-compose.yml)
# SECRET_KEY=dev-secret-change-in-production
# JWT_SECRET=
