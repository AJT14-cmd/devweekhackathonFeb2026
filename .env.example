# Project root .env — used by Docker Compose when you run from this directory.
# Compose passes these variables into the backend and frontend containers;
# you do NOT need to copy .env into backend/ or frontend/ when using Docker.
# Copy this file to .env and fill in your values.

# Deepgram API key (for live mic transcription)
# Example: DEEPGRAM_API_KEY=abc123def456789...
DEEPGRAM_API_KEY=your_deepgram_api_key_here

# You.com API key (for summarization: key insights, decisions, action items).
# Optional; without it, summary is first 500 chars of transcript.
# Get one at https://you.com/platform
# Example: YOUCOM_API_KEY=your_key_here
YOUCOM_API_KEY=

# MongoDB. Required for Docker: set one of the following (no spaces around =).
# Local (mongo container):  MONGODB_URI=mongodb://127.0.0.1:27017
# Atlas:  MONGODB_URI=mongodb+srv://USER:PASSWORD@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority
# (URL-encode password if it contains #, @, or =.)
# MONGODB_URI=mongodb://127.0.0.1:27017
# MONGODB_DB_NAME is empty by default; set only if you want a specific database name.
# MONGODB_DB_NAME=meeting_transcription

# Optional: override backend secrets (defaults are in docker-compose.yml)
# SECRET_KEY=dev-secret-change-in-production
# JWT_SECRET=
