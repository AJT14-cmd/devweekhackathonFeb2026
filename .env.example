# Insightly — Docker deployment config
# Copy to .env and fill in your values:  cp .env.example .env

# ─── Required for core features ─────────────────────────────────

# MongoDB connection
# Local (Docker): mongodb://mongo:27017
# Atlas: mongodb+srv://USER:PASS@cluster.xxxxx.mongodb.net/?retryWrites=true&w=majority
MONGODB_URI=mongodb://mongo:27017

# Deepgram API key — required for live mic + file transcription
# Get one: https://console.deepgram.com
DEEPGRAM_API_KEY=

# ─── Optional: AI & PDF ────────────────────────────────────────

# You.com — AI meeting summaries (key insights, decisions, action items)
# Get one: https://you.com/platform
# YOUCOM_API_KEY=

# Foxit — PDF meeting report generation
# Get credentials: https://developers.foxit.com
# FOXIT_CLIENT_ID=
# FOXIT_CLIENT_SECRET=
# FOXIT_PDF_SERVICES_CLIENT_ID=
# FOXIT_PDF_SERVICES_CLIENT_SECRET=
# FOXIT_HOST=https://na1.fusion.foxit.com

# ─── Optional: Production overrides ────────────────────────────

# Flask/JWT secrets (change in production)
# SECRET_KEY=dev-secret-change-in-production
# JWT_SECRET=

# When deploying: backend URL reachable from the browser
# VITE_API_URL=http://localhost:5000
# VITE_SOCKET_URL=http://localhost:5000
